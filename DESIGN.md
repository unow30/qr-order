# QR 코드 주문 시스템 설계 문서

## 1. 기획 배경

매장 내 주문·결제 솔루션으로 흔히 사용되는 키오스크나 전용 태블릿은 기기 구매·유지 비용이 상당하다. 반면 QR 코드 스티커는 각 테이블에 한 번 붙여두는 것만으로 고객 스마트폰을 주문 단말기로 활용할 수 있어, 초기 도입 비용을 크게 낮출 수 있다는 점에 착안하여 이 시스템을 기획하였다.

---

## 2. 초기 설계

테이블마다 고유한 QR 코드를 발급하고, 고객이 이를 스캔하면 바로 메뉴판에 접근할 수 있도록 하였다.

```
QR 스캔 → 메뉴 조회 → 장바구니 담기 → 주문하기 → 결제하기
```

접근에 별도 제한을 두지 않아 구현이 단순했지만, 이로 인해 아래와 같은 보안 이슈가 발견되었다.

---

## 3. 이슈 1 — 원거리 무단 주문

### 문제

QR 코드는 한 번 캡처하면 장소에 상관없이 스캔할 수 있다. 따라서 누군가 QR 코드를 사진으로 저장한 뒤 원거리에서 스캔하면, 실제로 자리에 앉아 있지 않은 상태에서도 주문·결제가 가능했다. 이 경우 손님이 없는 빈 테이블에 주문이 발생하거나, 다른 손님이 원치 않는 메뉴가 추가되는 문제가 생긴다.

### 검토한 해결안

| 방안 | 내용 | 기각 이유 |
|------|------|-----------|
| **NFC 칩 병행** | QR 코드와 NFC 칩을 함께 부착해, 물리적 근접 인증 필수 | NFC 칩 추가 비용 발생; 포트폴리오를 검토하는 외부인이 접근 불가 |
| **만료 QR 코드** | 일정 시간마다 QR 코드를 갱신해 탈취된 코드를 무력화 | 새 QR 코드를 인쇄해 교체해야 하는 번거로움; 비용 절감·편의성이라는 도입 취지와 충돌 |

### 채택 방안 — 세션 소유권

QR 코드 자체보다 **누가 먼저 스캔했는가**를 기준으로 접근권을 제어하는 방식을 선택하였다.

- QR 코드를 **처음 스캔한 사람**이 해당 테이블의 세션을 획득하고 소유자가 된다.
- 이후 장바구니 담기, 주문, 결제는 이 세션을 보유한 기기에서만 가능하다.
- 세션은 마지막 활동으로부터 **2시간** 동안 유지되며, 장바구니 추가나 주문 같은 활동이 발생하면 시간이 초기화된다.
- **결제가 완료되면 세션이 소멸**되어, 다음 손님이 새 세션을 획득할 수 있다.

---

## 4. 이슈 2 — 동행자 참여 제한

### 문제

세션 소유권 방식을 도입하자 새로운 문제가 생겼다. 같은 테이블에 앉은 동행자가 각자 메뉴를 고르고 주문하려면 세션 소유자에게 요청을 위임해야 했고, 이는 불편한 사용 경험으로 이어졌다.

### 해결 방안 — PIN 공유

- 세션 생성 시 **소유자 기기에만** 6자리 PIN 번호가 표시된다.
- 동행자가 같은 테이블의 QR 코드를 스캔하면, 메뉴판 대신 **PIN 입력 화면**이 표시된다.
- 소유자에게 PIN을 확인하여 입력하면 세션에 참여해 주문·결제를 독립적으로 진행할 수 있다.

이를 통해 각자 스마트폰으로 메뉴를 선택하고 주문하는 경험을 제공한다.

---

## 5. 잔존 이슈 — 빈 테이블 원거리 선점

세션 소유권 방식을 도입했음에도, **아무도 앉지 않은 빈 테이블**의 QR 코드를 원거리에서 스캔해 세션을 선점하는 상황은 여전히 가능하다.

이 경우 어드민 페이지에서 실제 테이블 상황과 일치하지 않는 주문을 직원이 확인하고 수동으로 취소하는 방식으로 대응한다. 완전한 자동화보다 **운영 인력을 통한 예외 처리**를 현실적인 해결책으로 채택하였다.

---

## 6. 이슈 3 — 자리 이동

### 문제

손님이 식사 중 다른 테이블로 이동하려 할 때 문제가 발생했다. 세션은 최초 스캔한 테이블과 1:1로 연결되어 있어, 다른 테이블로 이동하면 기존 세션으로는 새 테이블에 접근할 수 없었다. 이동 전까지 장바구니에 담아둔 내용이나 주문 내역이 원래 테이블에 귀속된 채로 남는 것이다.

### 해결 방안 — 세션 이동

손님이 이동하려는 테이블의 QR 코드를 스캔하면, 기존 세션을 해당 테이블로 이전하는 기능을 구현하였다.

- **장바구니와 주문 내역**이 세션과 함께 새 테이블로 이전된다.
- 이동 대상 테이블에 **다른 세션이 없을 때**만 이동이 허용된다 (충돌 방지).
- 다른 매장의 테이블로는 이동할 수 없다.

이를 통해 자리를 옮겨도 진행 중인 주문 과정이 끊기지 않는다.

---

## 7. 이슈 4 — 세션 만료 후 결제 불가

### 문제

세션 유효 시간(2시간)이 지나면 서버에서 세션이 삭제된다. 이 경우 이미 주문을 마친 손님이 결제하려 해도 세션 인증에 실패해 결제를 진행할 수 없는 상황이 생긴다.

### 해결 방안 — 이중 복구 경로

세션이 만료되더라도 주문 내역은 데이터베이스에 영구 보존된다는 점을 활용해, 두 가지 복구 경로를 마련하였다.

**경로 1 — 고객 기기 세션 스토리지 활용**

고객 기기의 세션 스토리지에는 서버 세션이 만료된 이후에도 세션 ID가 남아 있다. 이 세션 ID로 주문 테이블을 조회하면 이전 주문 내역을 불러올 수 있어, 고객에게 주문 확인 화면을 제공하고 결제를 이어서 진행할 수 있다.

**경로 2 — 어드민 결제 처리**

고객이 기기의 세션 스토리지를 직접 초기화하거나 브라우저를 닫아 세션 ID 자체가 사라진 경우에는 고객 측에서 주문 내역을 복구할 방법이 없다. 이때는 어드민 페이지에서 해당 테이블을 선택해 직원이 직접 결제를 처리하는 방식으로 대응한다.

| 상황 | 복구 경로 |
|------|-----------|
| 서버 세션 만료, 기기 세션 ID 보존 | 고객이 직접 주문 내역 확인 후 결제 |
| 서버 세션 만료 + 기기 세션 ID 소멸 | 어드민 페이지에서 직원이 테이블 결제 처리 |
