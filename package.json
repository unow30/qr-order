{
  "name": "qr-order",
  "version": "1.0.0",
  "description": "QR 테이블 오더 서비스 모노레포",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "dev:server": "pnpm --filter @qr-order/server dev",
    "dev:web": "pnpm --filter @qr-order/web dev",
    "dev:admin": "pnpm --filter @qr-order/admin dev",
    "type-check": "turbo run type-check",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean",
    "docker:up": "docker-compose -f docker-compose.local.yml up -d",
    "docker:down": "docker-compose -f docker-compose.local.yml down",
    "docker:db:seed": "pnpm --filter @qr-order/server seed",
    "docker:rebuild": "docker-compose -f docker-compose.local.yml down && docker-compose -f docker-compose.local.yml build server web admin && docker-compose -f docker-compose.local.yml up -d",
    "docker:restart": "docker-compose -f docker-compose.local.yml restart",
    "docker:restart:app": "docker-compose -f docker-compose.local.yml restart server web admin",
    "docker:restart:server": "docker-compose -f docker-compose.local.yml restart server",
    "docker:restart:web": "docker-compose -f docker-compose.local.yml restart web",
    "docker:restart:admin": "docker-compose -f docker-compose.local.yml restart admin",
    "docker:restart:nginx": "docker-compose -f docker-compose.local.yml restart nginx",
    "docker:restart:redis": "docker-compose -f docker-compose.local.yml restart redis",
    "docker:restart:postgres": "docker-compose -f docker-compose.local.yml restart postgres"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.5.3"
  }
}
